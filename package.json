{
  "name": "grammar-of-things",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "slop-audit": "node scripts/slop-audit.js",
    "deploy": "astro build && wrangler deploy --cwd dist/server --config wrangler.json"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "@astrojs/cloudflare": "^14.1.2",
    "@google/generative-ai": "^0.24.1",
    "astro": "^6.4.3",
    "marked": "^18.0.5",
    "openai": "^6.45.0",
    "wrangler": "^4.97.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
